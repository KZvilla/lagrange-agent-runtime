---
description: Architectural implementation plan from Antigravity - plan mode, asked not to change anything
---

Request an architectural and implementation plan from Google Antigravity CLI (`agy`):

Task:
$ARGUMENTS

Instructions:
1. Use the `lagrange_agy_plan` tool with `effort: "high"`.
2. Review the returned plan and present it clearly to the user.
3. Keep the `conversation_id` handy so the user can easily execute the plan with `/lagrange/run` or `lagrange_agy_run`.
